import React, { useEffect } from "react";
import { useParams } from "react-router-dom";

const ProductDetail = () => {
  const { id } = useParams;

  useEffect(() => {});
  return <div></div>;
};

export default ProductDetail;
